import { CreateChatRoomDTO } from "../shared/interface";

// create a chat room
export const handleCreateChatRoom = async(chatRoom: CreateChatRoomDTO) => {
  try {
    
  } catch (error) {
    
  }

}
// update a chat room with participants

// Change a chatroom name

// delete a chat room 